import React from "react";
import Building from "../../../../Building";

const Settings = () => {
	return (
		<div className="d-flex justify-content-center align-items-center">
			<Building width={250} height={250} />
		</div>
	);
};

export default Settings;
